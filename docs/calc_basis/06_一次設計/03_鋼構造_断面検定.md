# 6.3 鋼構造 断面検定

鋼構造の断面検定を、鋼構造設計規準・告示平13国交告第1024号に基づいて行う。

**算定式（梁）**

軸力・曲げによる応力度:

\\[ \sigma_{ax} = |N|/A,\quad \sigma_{by} = |M_z|/Z,\quad \sigma_{bz} = |M_y|/Z\_{\text{弱軸}} \\]

円形鋼管は強軸・弱軸の曲げモーメントを合成した \\( \sigma_b \\) で代表させる:

\\[ \sigma_b = \sqrt{M_z^2 + M_y^2}/Z \\]

軸力＋二軸曲げの組合せ検定。圧縮時（\\( f_c \\) は座屈を考慮した許容圧縮応力度）:

\\[ \sigma_c/f_c + \sigma_{by}/f_b + \sigma_{bz}/f_t \le 1.0 \\]

（円形鋼管は \\( \sigma_c/f_c + \sigma_b/f_b \\)）。引張時:

\\[ (\sigma_t + \sigma_{by} + \sigma_{bz})/f_t \le 1.0 \\]

（円形鋼管は \\( (\sigma_t + \sigma_b)/f_t \\)）。組合せ検定とは別に、単独曲げの検定 \\( \sigma_{by}/f_b \\)（円形鋼管は \\( \sigma_b/f_b \\) で代替）・\\( \sigma_{bz}/f_t \\) も行い、いずれも検定比として扱う。

許容圧縮応力度 \\( f_c \\) は、強軸・弱軸それぞれの座屈長さ（直接入力が無い場合は部材長）による細長比のうち大きい方で算定する:

\\[ \lambda = \max(l_{ky}/i_y,\ l_{kz}/i_z),\quad i_y = \sqrt{I_y/A},\quad i_z = \sqrt{I_z/A} \\]

（\\( l_{ky} \\): 強軸まわり座屈長さ、\\( l_{kz} \\): 弱軸まわり座屈長さ。両者とも部材長のとき \\( \lambda = l_k/i\_{\min} \\)、\\( i\_{\min} = \sqrt{\min(I_y, I_z)/A} \\) に一致する。）

**せん断の検定**

せん断応力度は \\( \tau_y = |Q_y|/A_y \le f_s \\)、\\( \tau_z = |Q_z|/A_z \le f_s \\) とする。せん断有効断面積 \\( A_y, A_z \\) は断面形状ごとに次式による:

- H 形: \\( A_y = t_w(H - 2t_f) \\)、\\( A_z = 2Bt_f/1.5 \\)
- 角形鋼管（角部外半径 \\( r \\) は断面定義時の入力値。\\( r=0 \\)（未入力）の場合は角部を直角とみなし \\( A_y = 2t(H-2t) \\)）: \\( r>0 \\) では \\( A_y = 2\{t(H-2r) + \pi t(2r-t)/4\} \\)（\\( A_z \\) は \\( H \\) を \\( B \\) に置換した同式）
- 円形鋼管: \\( A_y = A_z = \pi t (D - t)/2 \\)

**von Mises 合成**

H 形は強軸側・弱軸側の 2 式のうち大きい方を検定比とする:

\\[ \max\left(\dfrac{\sqrt{(\sigma_{ax}+\sigma_b')^2+3\tau_y^2}}{f_t},\ \dfrac{\sqrt{(\sigma_{ax}+\sigma_{by}+\sigma_{bz})^2+3\tau_z^2}}{f_t}\right) \\]

ここで \\( \sigma_b' = \sigma_{by}(H-2t_f)/H \\) はウェブ端の曲げ応力度である。角形鋼管（およびその他の形状）は次式による:

\\[ \sqrt{(\sigma_{ax}+\sigma_{by}+\sigma_{bz})^2+3\max(\tau_y,\tau_z)^2}/f_t \\]

円形鋼管は中立軸位置で検定する（曲げ応力度の寄与は無い）:

\\[ \sqrt{\sigma_{ax}^2+3(\tau_y^2+\tau_z^2)}/f_t \\]

梁の検定比は、上記の組合せ検定・単独曲げ検定・せん断検定・von Mises 合成の各比のうち最大値とする。

**許容曲げ応力度 fb（横座屈、H 形の強軸のみ考慮。他形状は \\( f_b = f_t \\)）**

旧基準（鋼構造設計規準 1973 年版）と新基準（AIJ 鋼構造許容応力度設計規準 2019 年版）の 2 通りの算定式を選択できる（既定は旧基準）。

旧基準:

\\[ f_b = \max(f_{b1}, f_{b2}) \le f_t \\]

\\[ f_{b1} = F(2/3 - (4/15)(l_b/i)^2/(C \cdot \Lambda^2)) \\]

\\[ f_{b2} = 89000/(l_b \cdot h/A_f) \\]

断面二次半径 \\( i \\) は、圧縮フランジと、せいが部材せいの 1/6 のウェブからなる T 形断面で算定する（ウェブ部の高さは \\( \max(h/6 - t_f,\ 0) \\)）。上下フランジの形状が異なる組立 H 形（BH）は、どちらが圧縮側か特定できないため、上下フランジそれぞれで求めた T 形断面のうち \\( i \\) が小さい側を安全側として採用する。

新基準（AIJ-ASD19）:

\\[ M_y = F \cdot Z,\quad M_e = C\sqrt{\dfrac{\pi^4 E I_z E I_w}{l_b^4} + \dfrac{\pi^2 E I_z G J}{l_b^2}} \\]

\\[ \lambda_b = \sqrt{M_y/M_e},\quad {}_e\lambda_b = 1/\sqrt{0.6},\quad \nu = 3/2 + (2/3)(\lambda_b/{}_e\lambda_b)^2 \\]

\\[ f_b = \begin{cases} F/\nu & (\lambda_b \le {}_p\lambda_b) \\\\ \left(1 - 0.4\dfrac{\lambda_b - {}_p\lambda_b}{{}_e\lambda_b - {}_p\lambda_b}\right)F/\nu & ({}_p\lambda_b < \lambda_b \le {}_e\lambda_b) \\\\ F/(2.17\lambda_b^2) & ({}_e\lambda_b < \lambda_b) \end{cases} \\]

長期の上限は \\( F/1.5 \\)。短期は長期の 1.5 倍（上限 \\( F \\)）とする。塑性限界細長比 \\( {}_p\lambda_b \\) は、横補剛区間内で曲げモーメントが最大となる場合は 0.3、区間内で単調に変化する場合は \\( 0.6+0.3(M_2/M_1) \\)（\\( M_2/M_1 \\) は複曲率で正・単曲率で負）とする。

横座屈修正係数（旧基準・新基準に共通）:

\\[ C = 1.75 + 1.05(M_2/M_1) + 0.3(M_2/M_1)^2 \le 2.3 \\]

横補剛により座屈区間 lb が部材の部分区間となる場合は、区間端モーメント比が不明なため安全側の \\( C = 1.0 \\) とする。

**算定式（柱・ブレース）**

柱（座屈考慮 fc）:

\\[ \sigma_c/f_c + \sigma_{bX}/f_{bX} + \sigma_{bY}/f_{bY} \le 1.0 \\]

- ブレース: 圧縮 \\( \sigma_c/f_c \\)、引張 \\( \sigma_t/f_t \\)

**実装**：`steel::{beam, column, brace, section}` が検定する。断面欠損を考慮した Z'、および横座屈長さ lb の解決も実装している。

## 6.3.1 柱の座屈長さ係数 K

柱の座屈長さ係数 K を、鋼構造塑性設計指針 式(6.65)〜(6.67)に基づいて算定する。
水平移動非拘束（sway）を前提とする。

**算定式**

\\[ \frac{G_A \cdot G_B \cdot x^2 - 36}{6(G_A + G_B)} = \frac{x}{\tan(x)} \\]

\\[ x = \pi/K \\]

（\\( K \ge 1 \\)）

\\[ G = \sum (EI/L)\_{\text{柱}}/\sum (EI/L)\_{\text{梁}} \\]

ピン端・梁無しは \\( G = 10 \\) とする。

**実装**：`steel::buckling`（`buckling.rs::sway_buckling_k`）が算定する。

## 6.3.2 S 造パネルゾーン

S 造パネルゾーンを、鋼構造接合部設計指針に基づいて検定する。

**算定式**

\\[ {}\_p M_y = (V_e/\kappa) \cdot \sqrt{1 - n^2} \cdot F_y/\sqrt{3} \\]

形状係数 κ（H形／角形／円形）、`Ve`（有効体積）。

**実装**：`steel::panel_zone`（`panel_zone.rs::s_panel_zone_check`）が検定する。

## 6.3.3 冷間成形角形鋼管柱の柱梁耐力比

冷間成形角形鋼管柱の柱梁耐力比を、2008年版「冷間成形角形鋼管の柱に用いる角形鋼管設計・施工マニュアル」に基づいて算定する。

**算定式**：耐力低減 `νu`（\\( n \le 0.5: 1 - 4n^2/3 \\)、\\( n > 0.5: 4(1-n)/3 \\)）、要求値 \\( \min(1.5 \cdot \sum M_{pb}, 1.3 \cdot M_{pp}) \\)。

**実装**：`steel::cold_formed`（`cold_formed.rs`）が算定する。
存在軸力は \\( N = N_L + 1.5 \cdot N_E \\) とする。
