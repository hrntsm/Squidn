# 5.3 固有値解析

固有周期とモードを、部分空間反復法（Bathe の定石）と一般化固有値問題の Jacobi 解法で求める。

**算定式**

部分空間サイズ（\\( p = \text{モード数} \\)）:

\\[ q = \min(2p, p+8) \\]

反復は次の予測から射影を作り、一般化固有値 `gevd_jacobi(K̄, M̄)` を解いて更新する:

\\[ y = K^{-1} \cdot x \\]

\\[ \bar{K} = y^\mathsf{T} K y, \bar{M} = y^\mathsf{T} M y \\]

\\[ x_{\text{new}} = y \cdot V \\]

収束は \\( |\theta - \theta_{\text{prev}}| < 10^{-10} \cdot \max(\theta, 1) \\) で判定する。周期および固有ベクトルの M 正規化は次のとおりとする:

\\[ \text{period} = 2\pi/\sqrt{\omega^2} \\]

\\[ \varphi^\mathsf{T} M \varphi = 1 \\]

参加係数および有効質量は次で算定する:

\\[ \beta = \frac{\varphi^\mathsf{T} M r}{\varphi^\mathsf{T} M \varphi} \\]

\\[ M_{\text{eff}} = \frac{(\varphi^\mathsf{T} M r)^2}{\varphi^\mathsf{T} M \varphi} \\]

**疎行列演算と計算コスト**

全体剛性行列 \\( K \\)・全体質量行列 \\( M \\) は、立体骨組ではほとんどの成分がゼロの疎行列になる（各自由度は同じ節点・隣接節点の自由度としか連成しないため）。固有値解析の反復に現れる行列演算は、この疎行列の**非ゼロ要素だけ**を用いて計算する:

1. **射影行列の算定**：部分空間への射影 \\( \bar{K} = Y^\mathsf{T} K Y \\)、\\( \bar{M} = Y^\mathsf{T} M Y \\) は、まず疎行列ベクトル積 \\( Z = K Y \\)（列ごとに非ゼロ要素のみを走査。コストは非ゼロ数 \\( \mathrm{nnz} \\) に比例）を求め、続いて \\( Y^\mathsf{T} Z \\) を計算する2段構成とする。1反復あたりのコストは \\( O(\mathrm{nnz} \cdot q + n q^2) \\)（\\( n \\): 自由度数、\\( q \\): 部分空間サイズ）で、モデル規模に対しほぼ線形にしか増えない。
2. **固有ベクトルの M 正規化・参加係数**：\\( \varphi^\mathsf{T} M \varphi \\) や \\( \varphi^\mathsf{T} M r \\) も同様に疎行列ベクトル積で算定する。
3. **分解の再利用**：反復中の \\( y = K^{-1} x \\) は、解析開始時に1回だけ行う剛性行列の疎 Cholesky 分解（[5.1 線形代数ソルバ](./01_線形代数ソルバ.md)）を（部分空間サイズ×反復回数）回の前進・後退代入で再利用する。静解析と続けて実行する場合は、静解析の準備で分解済みの剛性行列をそのまま使い回すため、固有値解析のための再分解は行わない。

このため固有値解析の所要時間は、モデル規模（自由度数と非ゼロ数）にほぼ比例して増える程度に収まり、小規模モデルでは即座に完了する。

**地震荷重（Ai 分布）との連携**

地震荷重の設計用一次固有周期 T の算定法（[1.1 地震力](../01_荷重/01_地震力.md)）で「精算（固有値解析）」を選択した場合、本解析で求めた 1 次モードの固有周期を用いる。固有周期は加力方向（X/Y）によらず同一のため、算定は 1 回で足りる。精算周期を使うには解析タブで固有値解析を先に実行しておく必要がある（未実行の場合は画面に案内が表示される）。既定の「略算」は告示の略算式 T = h(0.02+0.01α) を用いるため、固有値解析の実行は不要。

**実装**：`dynamic::eigen`（`eigen/mod.rs::solve_eigen` / `solve_eigen_with_solver`）が算定する。
`EIGEN_TOL = 1e-10`、`EIGEN_MAX_ITER = 200` とし、整合質量（Consistent）を使用する。
